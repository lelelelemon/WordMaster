package GUI;

public class Gui {
	public Gui(String libname,String type){
	}
	// drawBar(int one, int total)
	// drawPie(int one, int total)
	/*
	 * drawPicOne(WordList)
	 * 一轮背单词在背完所设数量单词后结束，显示本次背单词的统计信息，包括
	 * 所选词库名、
	 * 所选单词数量、
	 * 正确单词数、
	 * 错误单词数、
	 * 正确率，
	 * 只需要表格形式展现。
	 */
	/* drawPic(WordList)
	 * 另外在主界面用户可以不选择单词背诵而是查看所选词库的统计信息，点击后显示相应词库背诵情况，包括
	 * 词库名、
	 * 词库单词总数、
	 * 已背单词数、
	 * 正确单词数
	 * 错误单词数、
	 * 正确率。
	 * 3 types */
}
